# Specs

This directory holds the source-of-truth domain specs for `virtual-office`.

Each domain gets a subdirectory: `openspec/specs/{domain}/spec.md`.

No domain specs exist yet — the project is greenfield. `sdd-archive` populates
this directory by merging delta specs from completed changes.
