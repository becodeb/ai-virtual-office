{
  "name": "@virtual-office/hooks",
  "private": true,
  "version": "0.0.0",
  "description": "Claude Code lifecycle hooks that feed the office hub.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@virtual-office/shared": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
