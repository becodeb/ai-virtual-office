{
  "name": "assets-pipeline",
  "private": true,
  "version": "0.0.0",
  "description": "Offline build-time pipeline: FBX/GLB character skins + UAL1/UAL2 animation packs -> browser-ready GLBs, retargeted onto the shared Quaternius rig.",
  "type": "module",
  "scripts": {
    "build:assets": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@virtual-office/shared": "workspace:*",
    "three": "0.180.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "^0.180.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
