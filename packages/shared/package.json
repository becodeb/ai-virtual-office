{
  "name": "@virtual-office/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Types shared by the hooks, server, client and assets-pipeline workspaces.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
