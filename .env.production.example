# Production. Copy to .env.production (gitignored) and adjust.
#
# No host port here on purpose: production publishes nothing and is reached
# through a reverse proxy. See docker-compose.prod.yml.

# "true" strips every task and prompt summary from the character labels.
# Recommended for any office that is visible beyond the people generating it.
OFFICE_REDACT_PROMPTS=false
